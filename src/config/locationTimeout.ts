/**
 * 위치 취득 대기 한계 — 한 곳에서 정한다.
 *
 * 왜 있나 (2026-09-10):
 *   `expo-location`의 `getCurrentPositionAsync()`에는 **타임아웃 옵션이 없다.**
 *   부르는 쪽이 걸지 않으면 GPS fix가 안 잡히는 곳(실내·지하)에서 영영 돌아오지 않는다.
 *   실제로 탐색 지도의 「현위치」 버튼이 그 상태로 잠겨 죽은 버튼이 됐다는 신고가 있었다.
 *   앱 전체에서 `getCurrentPositionAsync`를 부르는 5곳 모두 타임아웃이 없었다.
 *
 * 값을 나눈 이유 — 기다림의 대가가 자리마다 다르다:
 *   USER_ACTION  버튼을 눌러 기다리는 중이다. 화면이 멈춘 것처럼 보이므로 짧게 끊는다.
 *   CRITICAL     발도장 근접 검증. 정확도가 판정을 좌우하니 조금 더 준다.
 *                실패해도 마지막 좌표로 폴백하고 서버가 좌표의 나이를 보고 판단한다.
 *   BACKGROUND   앱 진입 시 조용히 잡는다. 실패해도 홈은 서버 폴백으로 채워진다.
 *                사용자가 기다리는 화면이 아니므로 넉넉해도 되지만, 매달려 있을 이유는 없다.
 *
 * 늘리기 전에: 오래 기다린다고 fix가 잡히지는 않는다. 실내면 30초를 줘도 못 잡는다.
 *              그 시간만큼 사용자가 멈춘 화면을 본다.
 */

export const LOCATION_TIMEOUT_MS = {
  /** 사용자가 버튼을 누르고 기다리는 중 (탐색 현위치, 위치 새로고침) */
  USER_ACTION: 8_000,
  /** 발도장 근접 검증 — 정확도가 중요해 조금 더 기다린다 */
  CRITICAL: 12_000,
  /** 앱 진입 시 백그라운드 취득 — 실패해도 화면이 막히지 않는다 */
  BACKGROUND: 10_000,
} as const;
