/**
 * 표준 문구 사전 (SSOT)
 *
 * 근거: docs/planning/19.DogEar(메시지 체계 V&N).md §4 — 화면마다 문구를 새로 짓지 않는다.
 * 문서와 코드가 갈라지는 걸 막으려고 사전을 코드에 박았다. **문구를 바꾸면 문서 §4도 함께 고친다.**
 *
 * 톤: "침착한 산책 동반자"(문서 §3.0)
 *   · 사실 먼저, 감정은 절제 · 강아지는 따뜻하게, 시스템은 담백하게
 *   · 책임은 서비스가 · 다음 행동을 항상 준다
 *
 * 문장부호 규칙(§3.1): **토스트는 마침표 없이 짧게**, Alert·인라인은 마침표를 쓴다.
 */

/** 지원 창구 — 개인 이메일 노출 금지(§1.1 ④) */
export const SUPPORT_EMAIL = 'support@9factorial.com';

// 4.1 공통 오류는 **일부러 상수로 두지 않았다.**
//   실제로 붙여보니 화면마다 "무엇에 실패했는지"가 달라서
//   ('사진만 올리지 못했어요', '장소를 제안하지 못했어요' …) 공통 문구를 쓸 자리가 없었다.
//   대신 형태를 지킨다 — **무엇이 실패했는지 + 다음 행동**, 마침표 없이.

// ─── 4.2 인증 (Alert — §2.2 예외, 흐름 차단 유지) ─────────
export const AUTH = {
  /** 소셜 로그인 실패는 provider만 바꿔 **이 한 문장으로 통일**한다(현행 문구 난립 제거) */
  socialFailed: (provider: string) => `${provider} 로그인에 실패했어요. 잠시 후 다시 시도해주세요.`,
  /** 토큰을 못 받은 경우도 사용자 입장에선 같은 실패다 → 같은 문구를 쓴다 */
  emailFailed: '로그인하지 못했어요. 이메일과 비밀번호를 확인해주세요.',
  playServices: 'Google Play 서비스를 사용할 수 없어요. 다른 방법으로 로그인해주세요.',
} as const;

// ─── 4.3 권한 (Alert + 설정 이동) ────────────────────────
export const PERM = {
  photo: '사진 첨부에는 갤러리 권한이 필요해요.',
  notification: '설정에서 DogEar의 알림을 허용해주세요.',
  notificationOff: '알림 해제는 시스템 설정에서 바꿀 수 있어요.',
} as const;

// ─── 4.4 입력 검증 (인라인) ──────────────────────────────
/** 인라인은 마침표를 쓰지 않는다 — 필드 바로 아래 짧은 한 줄이라 문장이 아니다 */
export const VALID = {
  required: (field: string) => `${field}을(를) 입력해주세요`,
  email: '올바른 이메일을 입력해주세요',
  password: '비밀번호를 입력해주세요',
  passwordShort: '비밀번호는 8자 이상이어야 해요',
  passwordMismatch: '비밀번호가 일치하지 않아요',
} as const;

// ─── 4.5 성공 (토스트 — 마침표 없음) ─────────────────────
export const OK = {
  dogCreated: (name: string) => `${name}를 등록했어요`,
  report: '신고를 접수했어요. 운영자가 검토할게요',
  unblocked: '차단을 해제했어요',
} as const;

/**
 * 장소 저장(하트) 토글에는 성공 토스트를 **일부러 두지 않는다.**
 * 아이콘이 그 자리에서 바뀌는 게 이미 피드백이다 — 토스트를 얹으면 소음이 된다.
 * 대신 **실패했을 때만** 알린다(낙관적 변경을 되돌리므로 침묵하면 사용자가 속는다).
 */
export const SAVE_SPOT_FAILED = {
  save: '저장하지 못했어요. 잠시 후 다시 시도해주세요',
  unsave: '저장 해제를 하지 못했어요. 잠시 후 다시 시도해주세요',
} as const;

// ─── 4.6 인사 (토스트) ───────────────────────────────────
export const GREET = {
  new: (name: string) => `반갑습니다, ${name}님`,
  returning: (name: string) => `다시 오셨네요, ${name}님`,
} as const;

// 4.7 빈 상태도 상수로 두지 않는다.
//   제목 + 다음 행동 + (때로) CTA가 한 덩어리라 화면마다 조립이 다르다.
//   재사용되는 건 문자열이 아니라 **형식**이다 — 문서 19 §4.7의 두 줄 규칙을 따른다.

// ─── 4.8 사진·갤러리 ─────────────────────────────────────
export const PHOTO = {
  deleted: '사진을 삭제했어요',
  deleteConfirm: '장소 갤러리에서 사라지고 되돌릴 수 없어요. 발도장 기록은 그대로 남아요.',
  uploadFailed: '사진을 올리지 못했어요. 다시 시도하거나 사진을 빼주세요.',
  loadFailed: '사진을 불러오지 못했어요. 잠시 후 다시 시도해주세요.',
} as const;

// ─── 4.9 발도장 ──────────────────────────────────────────
// 성공은 토스트가 아니라 **전용 완료 화면**이 맡는다(paw-checkin의 isSuccess).
// 근접·쿨다운·정확도 안내도 여기 두지 않는다 — 화면 패널이 거리·허용반경·남은 시간까지
// 넣어 훨씬 자세히 말하고 있어서, 한 줄짜리 사본을 두면 반드시 둘이 갈라진다.
export const PAW = {
  deleted: '발도장을 삭제했어요',
} as const;

// ─── 4.10 되돌릴 수 없는 작업 (Alert 유지 — §2.2) ────────
// 강아지·계정 삭제는 각 화면이 더 구체적으로 쓴다(강아지 이름, 사라지는 항목 목록).
// 여기엔 화면 간에 실제로 공유되는 것만 남긴다.
export const DANGER = {
  checkinDelete: '이 발도장과 함께 올린 사진도 모두 삭제돼요. 되돌릴 수 없어요.',
} as const;
